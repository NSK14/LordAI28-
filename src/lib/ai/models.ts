export type LordMode = "fast" | "balanced" | "coding" | "creative" | "reasoning" | "local";

export const LORD_MODES: readonly LordMode[] = [
  "fast",
  "balanced",
  "coding",
  "creative",
  "reasoning",
  "local",
];

/** User-facing routing choices. Provider model IDs intentionally do not live here. */
